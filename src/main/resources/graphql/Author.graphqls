type Author {
    id: ID!
    name: String!
    age: Int
}

# root
type Query {
    findAllAuthors: [Author]!
    countAuthors: Int
}

type Mutation {
    createAuthor(name: String!, age: Int): Author!
}